Patent API
API для обработки и управления патентными документами в формате PDF.

📋 Описание
Patent API — это FastAPI-приложение для загрузки, проверки и извлечения текста из патентных PDF-файлов. Сервис предоставляет RESTful API для интеграции с фронтенд-приложениями (например, Angular).

✨ Основные возможности
📄 Загрузка PDF-файлов патентов через API

🔍 Автоматическая проверка формата файлов (сигнатура PDF)

📝 Извлечение текста из PDF без сохранения на диск

🔒 CORS настройка для безопасной работы с фронтендом

📊 Метаданные обработки файлов

🐛 Обработка ошибок с детальными сообщениями

🚀 Быстрый старт
Предварительные требования
Python 3.8+

pip (менеджер пакетов Python)

Установка
Клонируйте репозиторий:

bash
git clone <repository-url>
cd patent-api
Установите зависимости:

bash
pip install -r requirements.txt
Запустите приложение:

bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
Или используйте Docker:
bash
docker build -t patent-api .
docker run -p 8000:8000 patent-api
📁 Структура проекта
text
patent-api/
├── main.py              # Основной файл FastAPI приложения
├── utils/
│   └── pdf_text_extractor.py  # Модуль для извлечения текста из PDF
├── requirements.txt     # Зависимости Python
├── README.md           # Эта документация
└── tests/              # Тесты (рекомендуется добавить)
🔧 Зависимости
Основные зависимости указаны в requirements.txt:

fastapi>=0.122.0 - Веб-фреймворк

uvicorn[standard]>=0.24.0 - ASGI сервер

pdfplumber - Извлечение текста из PDF

pylint>=3.0.0 - Линтинг кода

pycodestyle>=2.11.0 - Проверка стиля кода

🌐 API Endpoints
1. GET /
Проверка работоспособности API.

Ответ:

json
{
  "message": "Hello World"
}
2. POST /patent
Загрузка и обработка PDF-файла патента.

Параметры:

file: PDF файл (multipart/form-data)

Успешный ответ:

json
{
  "message": "Файл патента успешно получен и обработан",
  "status": "processed",
  "extracted_text": "Извлеченный текст из PDF...",
  "metadata": {}
}
Ошибки:

400: Неверный формат файла

500: Ошибка при обработке PDF

🛠 Использование
Пример запроса с cURL:
bash
curl -X POST "http://localhost:8000/patent" \
  -H "accept: application/json" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@path/to/patent.pdf"
Пример запроса с Python (requests):
python
import requests

url = "http://localhost:8000/patent"
files = {"file": open("patent.pdf", "rb")}

response = requests.post(url, files=files)
print(response.json())
Интеграция с Angular:
typescript
import { HttpClient } from '@angular/common/http';

uploadPatent(file: File) {
  const formData = new FormData();
  formData.append('file', file);
  
  return this.http.post('http://localhost:8000/patent', formData);
}
🔍 Особенности обработки PDF
Модуль pdf_text_extractor.py:

✅ Не сохраняет файлы на диск - обработка в памяти

✅ Проверяет сигнатуру PDF (%PDF)

✅ Извлекает текст без форматирования

✅ Объединяет все страницы в одну строку

✅ Обработка больших файлов через потоковое чтение

⚙️ Конфигурация CORS
Приложение настроено для работы с Angular фронтендом:

python
allow_origins=["http://localhost:4200"]  # Angular dev server
Для продакшена измените на ваш домен.

🧪 Тестирование
Запуск тестов:
bash
# Установите тестовые зависимости
pip install pytest pytest-asyncio httpx

# Запустите тесты
pytest
Пример теста:
python
import pytest
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)

def test_root_endpoint():
    response = client.get("/")
    assert response.status_code == 200
    assert response.json() == {"message": "Hello World"}
📊 Мониторинг и документация
Приложение предоставляет автоматическую документацию:

Swagger UI: http://localhost:8000/docs

ReDoc: http://localhost:8000/redoc

🔒 Безопасность
Валидация MIME-типов файлов

Проверка сигнатуры PDF

Ограничение размеров файлов (можно настроить в FastAPI)

CORS для контроля доступа

🚨 Обработка ошибок
API возвращает понятные сообщения об ошибках:

Неверный PDF файл - если файл не PDF

Файл не похож на PDF - если нет сигнатуры %PDF

Произошла ошибка при обработке PDF - внутренние ошибки

📈 Расширение функциональности
Рекомендуемые улучшения:

Добавить поддержку:

OCR для сканированных PDF

Извлечение изображений и таблиц

Анализ структуры патента

Улучшить обработку:

Кэширование результатов

Очередь задач для больших файлов

Логирование операций

Добавить endpoints:

/patents - список обработанных патентов

/patent/{id} - получение конкретного патента

/search - поиск по тексту патентов

🤝 Вклад в проект
Форкните репозиторий

Создайте ветку для фичи

Коммитьте изменения

Отправьте Pull Request

Стиль кода:
bash
# Проверка стиля
pycodestyle --max-line-length=100 main.py utils/

# Линтинг
pylint main.py utils/
📄 Лицензия
[Укажите вашу лицензию]

👥 Контакты
Автор: [Ваше имя/команда]

Issues: [Ссылка на трекер задач]

Документация: [Дополнительная документация]

🌟 Поддержка
Если вы столкнулись с проблемами:

Проверьте, что файл действительно PDF

Убедитесь, что файл не поврежден

Проверьте размер файла (большие файлы могут требовать больше памяти)

Посмотрите логи сервера для деталей ошибки

Примечание: Для продакшн-использования рекомендуется настроить:

HTTPS

Аутентификацию

Лимиты запросов

Мониторинг

Резервное копирование
